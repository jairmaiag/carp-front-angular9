import { CabecalhoOrdenacaoDirective } from './cabecalho-ordenacao.directive';

describe('CabecalhoOrdenacaoDirective', () => {
  it('should create an instance', () => {
    const directive = new CabecalhoOrdenacaoDirective();
    expect(directive).toBeTruthy();
  });
});
